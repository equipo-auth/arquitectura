import OrganizadorPlaceholder from "@/modules/organizador/components/OrganizadorPlaceholder";

// Página de "Panel Organizador" — GRUPO 7
//
// Este archivo debería ser corto: solo importa y compone componentes
// desde src/modules/organizador/. Toda la lógica (llamadas a la API,
// estado, formularios, etc.) vive en esa carpeta, NO acá.

export default function OrganizadorPage() {
  return <OrganizadorPlaceholder />;
}
