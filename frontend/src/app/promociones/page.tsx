import PromocionesPlaceholder from "@/modules/promociones/components/PromocionesPlaceholder";

// Página de "Promociones" — GRUPO 9
//
// Este archivo debería ser corto: solo importa y compone componentes
// desde src/modules/promociones/. Toda la lógica (llamadas a la API,
// estado, formularios, etc.) vive en esa carpeta, NO acá.

export default function PromocionesPage() {
  return <PromocionesPlaceholder />;
}
