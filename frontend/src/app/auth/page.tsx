import AuthPlaceholder from "@/modules/auth/components/AuthPlaceholder";

// Página de "Autenticación (Auth)" — GRUPO 1
//
// Este archivo debería ser corto: solo importa y compone componentes
// desde src/modules/auth/. Toda la lógica (llamadas a la API,
// estado, formularios, etc.) vive en esa carpeta, NO acá.

export default function AuthPage() {
  return <AuthPlaceholder />;
}
